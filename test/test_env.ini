# -------------------------------------------------
# Пример с использованием переменных окружения
# -------------------------------------------------

database = ${DATABASE}

[server]
port = ${PORT}
host = ${HOSTNAME}
